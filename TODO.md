# Wallet Routes Startup Error Fix\n\n## Steps\n\n✅ **Step 1:** Fixed destructuring import in `backend/src/routes/tenant/wallet.routes.js`\n  - Changed `const { tenantAuthMiddleware } = require(...)` to `const tenantAuthMiddleware = require(...)`\n\n⏳ **Step 2:** Restart backend server\n  Run: `docker-compose restart backend`\n\n⏳ **Step 3:** Test wallet endpoint\n  GET `/api/tenant/wallet/balance` with valid ServiceProvider auth token
